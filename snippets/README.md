# Snippets

This is a code snippet vault.

Keep snippet files directly in this folder so they are easy to search.

Copy snippets into a specific site's own `src/script.js` or `src/styles.css` file before adapting them. Live Webflow sites should not import files from this folder directly.
